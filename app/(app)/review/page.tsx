import { PhaseNotice } from "@/components/phase-notice";

export default function ReviewPage() {
  return (
    <PhaseNotice title="Review" phase="Phase 3 / Phase 5">
      The accuracy-ratio chart trending toward 1.0, hours per bucket, per-category
      calibration, and the defer leaderboard.
    </PhaseNotice>
  );
}
