import { PhaseNotice } from "@/components/phase-notice";

export default function TodayPage() {
  return (
    <PhaseNotice title="Today" phase="Phase 2 — compose and the ribbon">
      This is where the day ribbon and “Plan my day” will live. For now, add and
      confirm tasks in the inbox and set your day profile in settings.
    </PhaseNotice>
  );
}
